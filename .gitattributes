# Переводы строк в репозитории всегда LF, в рабочей копии — родные для системы.
# Без этого Windows коммитит CRLF, и на Linux/macOS файл выглядит изменённым целиком.
* text=auto eol=lf

# Бинарники не трогать
*.jpg binary
*.png binary
*.mp4 binary
